# domain-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-api` to build the library.

## Running unit tests

Run `nx test domain-api` to execute the unit tests via [Jest](https://jestjs.io).
