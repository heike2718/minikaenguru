# domain-data

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-data` to build the library.

## Running unit tests

Run `nx test domain-data` to execute the unit tests via [Jest](https://jestjs.io).
